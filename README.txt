required installing to implment pycaret:

pip install pycaret
pip install markupsafe==2.0.1
pip install numba --upgrade



In the beginning, we try to clean the dataset and get the results,
which are used in the presentation.
but according to the feedback during the presentation, we should improve the accuracy.
so, we trying to use different ways to clean the data and feature engineering.
according to this, we get different results from the presentation.
 
